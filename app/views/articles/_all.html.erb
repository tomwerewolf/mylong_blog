<h1><%= t("articles.views.index.head") %></h1>

<div class="row">
  <div class="leftcolumn">
    <div class="card">
      <% articles.each do |article| %>
      <h2><%= link_to article.title, article %></h2>
      <%# <div class="fakeimg">
      <% if article.image.attached? %>
        <%#= image_tag article.image, height: 300 %>
      <%# end %>
      <%#/div> %>
      <p><%= article.body %></p>
      <% end %>
      <%= render "shared/page", resource: articles %>
    </div>
  </div>
</div>    