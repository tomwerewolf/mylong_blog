<%= form_with model: [:admin, category], data: { turbo: false } do |form| %>
  <%= render "shared/error_messages", resource: category %>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>