<!DOCTYPE html>
<html>
  <head>
    <title>MylongBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>                        
          </button>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
          <ul class="nav navbar-nav">
            <li class="active"><%= link_to t("sessions.current.home"), root_path %></li>
            <% Category.order(:name).each do |cat| %>
              <li>
                <%= link_to cat.name, category_posts_path(cat) %>
              </li>
            <% end %>  
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if logged_in? %>
              <li><%= link_to t("articles.views.my_post"), my_posts_articles_path %></li>
              <li><%= link_to current_user.username, profile_path %></li>
              <li><%= link_to t("sessions.current.logout"), logout_path, data: { turbo_method: :delete } %></li>
            <% else %>
              <li><%= link_to t("sessions.views.register"), new_user_path %></li>
              <li><%= link_to t("sessions.views.login"), login_path %></li>
          </ul>
          <% end %>
        </div>
      </div>
    </nav>

    <%= yield %>

    <footer class="footer">
      <div class="div-footer">
        <%= link_to t("sessions.current.eng"), locale:"en" %> |
        <%= link_to t("sessions.current.viet"), locale:"vi" %>
      </div>
    </footer>
  </body>
</html>
